package com.technostack.stream.funInterface.consumer;

public class ConsumerAdvance {

    // 🌳 Advanced Level
    //	1.	Design a reusable Consumer pipeline that handles error logging and retry logic.
    //	2.	Implement a Map<String, Consumer<Order>> for different order states ("PLACED", "SHIPPED", etc.).
    //	3.	Write a Consumer that batches database updates (simulate via list).
    //	4.	Combine Predicate and Consumer: only apply consumer if predicate passes.
    //	5.	Use a Consumer in a custom functional pipeline (e.g., data enrichment + logging).
}
